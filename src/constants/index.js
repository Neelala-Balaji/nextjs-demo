import {
  PATH_NAMES,
  LANGUAGE_KEYS,
  Languages,
  DEFAULT_LOCALE,
  LOCALE_PREFIX,
} from "./configParms";
import { URL } from "./url";
import { PAGES, CATALOG_DEFAULT_PARMAS } from "./appPages";

export {
  PATH_NAMES,
  LANGUAGE_KEYS,
  Languages,
  URL,
  PAGES,
  CATALOG_DEFAULT_PARMAS,
  DEFAULT_LOCALE,
  LOCALE_PREFIX,
};
